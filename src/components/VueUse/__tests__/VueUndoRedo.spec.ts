import { describe, it } from 'vitest'

describe('VueUndoRedo', () => {
  it.todo('displays the right choices for the given step')
  it.todo('displays the result when all the steps are done')
  it.todo('goes back to the previous step when clicking on undo')
  it.todo('goes back to the current step when clicking on redo')
})
